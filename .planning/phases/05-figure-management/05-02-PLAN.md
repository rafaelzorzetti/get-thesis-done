---
phase: 05-figure-management
plan: 02
type: execute
wave: 2
depends_on: [05-01]
files_modified:
  - get-thesis-done/workflows/add-figure.md
  - commands/gtd/add-figure.md
autonomous: true
requirements: [FIG-01, FIG-02, FIG-03, FIG-04, FIG-05]

must_haves:
  truths:
    - "User can run /gtd:add-figure with an ID, type, chapter, and caption and the figure is registered in FIGURES.md"
    - "Workflow creates appropriate source file scaffold based on figure type (excalidraw JSON, tikz template, or static placement instructions)"
    - "Workflow generates the correct LaTeX snippet for the figure type using abnTeX2 pattern (caption before image, legend after)"
    - "Workflow runs validate-figs after registration to check overall figure reference health"
    - "Workflow commits FIGURES.md and source file changes to git"
  artifacts:
    - path: "get-thesis-done/workflows/add-figure.md"
      provides: "Figure registration workflow with source scaffold creation and LaTeX snippet generation"
      min_lines: 150
    - path: "commands/gtd/add-figure.md"
      provides: "Command definition exposing /gtd:add-figure to user"
      contains: "gtd:add-figure"
  key_links:
    - from: "commands/gtd/add-figure.md"
      to: "get-thesis-done/workflows/add-figure.md"
      via: "execution_context @-reference"
      pattern: "workflows/add-figure"
    - from: "get-thesis-done/workflows/add-figure.md"
      to: "gtd-tools.js register-figure"
      via: "Bash node gtd-tools.js register-figure"
      pattern: "register-figure"
    - from: "get-thesis-done/workflows/add-figure.md"
      to: "gtd-tools.js validate-figs"
      via: "Bash node gtd-tools.js validate-figs"
      pattern: "validate-figs"
---

<objective>
Create the add-figure workflow and /gtd:add-figure command that orchestrate the CLI tools from Plan 01 into a user-facing figure management experience with registration, source file scaffolding, LaTeX snippet generation, validation, and git commit.

Purpose: Wire the register-figure and validate-figs CLI commands into a cohesive workflow that guides the user through adding a figure to their thesis. This is the user-facing layer -- Plan 01 built the tools, Plan 02 makes them accessible.
Output: get-thesis-done/workflows/add-figure.md (~200-300 lines) + commands/gtd/add-figure.md (~40-50 lines)
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-figure-management/05-RESEARCH.md
@.planning/phases/05-figure-management/05-01-SUMMARY.md

@get-thesis-done/workflows/add-reference.md
@commands/gtd/add-reference.md
@agents/figure-manager.md

<interfaces>
<!-- CLI commands available from Plan 01 (all in gtd-tools.js) -->

register-figure --id <id> --type <excalidraw|tikz|static> --chapter <N> --caption <text> [--raw]
  Input: figure ID (kebab-case enforced), type, chapter number, caption text
  Output JSON: { id, caption, chapter, type, sourceFile, status, catalogPath }
  Raw: "Registered figure '{id}' (type: {type}) in FIGURES.md for chapter {chapter}"
  Errors: missing args, invalid type, duplicate ID, FIGURES.md not found

validate-figs [--raw]
  Input: none (scans all chapters and FIGURES.md automatically)
  Output JSON: { total_catalog_entries, total_referenced_ids, missing_from_catalog, unreferenced_in_catalog, labeled_not_in_catalog, per_chapter }
  Raw: formatted report with per-chapter breakdown

<!-- LaTeX figure patterns (from 05-RESEARCH.md) -->

Excalidraw/Static figure (abnTeX2):
\begin{figure}[htb]
  \caption{\label{fig:FIGURE_ID}CAPTION_TEXT}
  \begin{center}
    \includegraphics[width=0.8\textwidth]{exports/FIGURE_ID}
  \end{center}
  \legend{Fonte: os autores}
\end{figure}

TikZ figure (abnTeX2):
\begin{figure}[htb]
  \caption{\label{fig:FIGURE_ID}CAPTION_TEXT}
  \begin{center}
    \input{figures/FIGURE_ID.tikz}
  \end{center}
  \legend{Fonte: os autores}
\end{figure}

<!-- Command pattern from commands/gtd/add-reference.md -->
Frontmatter: name, description, argument-hint, allowed-tools
Sections: <objective>, <execution_context>, <context>, <process>

<!-- Workflow pattern from get-thesis-done/workflows/add-reference.md -->
Sections: <purpose>, <core_principle>, <terminology>, <process> with numbered <step> elements
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create add-figure workflow</name>
  <files>get-thesis-done/workflows/add-figure.md</files>
  <action>
Create `get-thesis-done/workflows/add-figure.md` following the exact structural pattern of `add-reference.md` (purpose, core_principle, terminology, process with steps).

**<purpose> section:**
Orchestrate figure registration, source file scaffolding, and LaTeX snippet generation for the thesis. Receive figure details from the user (ID, type, chapter, caption), delegate registration to the CLI, create the appropriate source file scaffold, generate the LaTeX inclusion snippet, run validation, and commit changes. Present all results to the researcher with clear next steps.

**<core_principle> section:**
CLI commands do the mechanical work (register in FIGURES.md, validate references). This workflow orchestrates the sequence: gather figure details, register via CLI, create source files, generate LaTeX snippets, validate, and commit. The figure-manager agent is not spawned for simple registrations -- the workflow handles the complete flow directly. The agent is available for complex validation scenarios (future enhancement).

**<terminology> section:**
Include the thesis-native terminology table (same pattern as add-reference.md):
| This Workflow Says | NOT This |
|--------------------|----------|
| figure | image asset |
| figure catalog | figure database |
| FIGURES.md | figure registry |
| caption | description |
| thesis | book |
| chapter | phase |
| researcher | author (literary) |
| FRAMEWORK.md | BIBLE.md |
| STRUCTURE.md | OUTLINE.md |
| gtd-tools.js | gwd-tools.js |
| get-thesis-done | get-writing-done |
| /gtd:* | /gwd:* |

**<process> section with the following steps:**

**Step 1: Gather Figure Details** (priority: first)
- Receive arguments from user via `/gtd:add-figure`
- Parse the following from $ARGUMENTS or ask the user if missing:
  1. **ID** (required): The figure identifier. Will be normalized to kebab-case. Ask: "What ID should this figure have? (e.g., system-architecture, data-flow-diagram)"
  2. **Type** (required): One of `excalidraw`, `tikz`, or `static`. Ask: "What type of figure? Options: excalidraw (hand-drawn diagram), tikz (LaTeX-native precise diagram), static (existing image file PNG/PDF/JPG)"
  3. **Chapter** (required): Which chapter this figure belongs to. Ask: "Which chapter will this figure appear in? (number)"
  4. **Caption** (required): The figure caption text. Ask: "What caption should this figure have?"
- If the user provides all details inline (e.g., `/gtd:add-figure system-arch --type excalidraw --chapter 3 --caption "System architecture overview"`), parse them directly without asking.
- If arguments are partially provided, only ask for the missing ones.
- Validate:
  - Type must be one of: excalidraw, tikz, static
  - Chapter must be a positive integer
  - ID will be auto-normalized (inform user: "ID normalized to: {kebab-case-id}")

**Step 2: Register Figure in Catalog**
Run the register-figure CLI command:
```bash
node ~/.claude/get-thesis-done/bin/gtd-tools.js register-figure --id "{id}" --type "{type}" --chapter {chapter} --caption "{caption}" --raw
```
- Parse the output. Report registration success to the user.
- If the CLI reports a duplicate ID error, inform the user and ask for a different ID.
- If FIGURES.md is not found, instruct the user to run `/gtd:new-thesis` first.

**Step 3: Create Source File Scaffold**
Based on the figure type, create the appropriate source file in `src/figures/`:

For **excalidraw** type:
- Create `src/figures/{id}.excalidraw` with a valid empty Excalidraw JSON:
```json
{
  "type": "excalidraw",
  "version": 2,
  "source": "https://excalidraw.com",
  "elements": [],
  "appState": {
    "gridSize": null,
    "viewBackgroundColor": "#ffffff"
  },
  "files": {}
}
```
- Also create the `src/figures/exports/` directory if it doesn't exist (for future exports).
- Tell the user: "Created empty Excalidraw canvas at src/figures/{id}.excalidraw. Open this file in Excalidraw (https://excalidraw.com or VS Code extension) to create your diagram. The figure will be automatically exported to PDF during compilation (`/gtd:compile`)."

For **tikz** type:
- Create `src/figures/{id}.tikz` with a template:
```latex
% TikZ figure: {caption}
% This file is included via \input{figures/{id}.tikz} in the chapter
\begin{tikzpicture}
  % TODO: Add your TikZ drawing here
  % Example: \draw (0,0) rectangle (4,3);
  % Example: \node at (2,1.5) {Placeholder};
  \node[draw, rounded corners, minimum width=4cm, minimum height=2cm] {TODO: {caption}};
\end{tikzpicture}
```
- Tell the user: "Created TikZ template at src/figures/{id}.tikz. Edit this file to create your diagram. TikZ figures compile inline with LaTeX -- no export step needed."

For **static** type:
- Create the `src/figures/` directory if it doesn't exist.
- Do NOT create a placeholder image file.
- Tell the user: "Place your image file at src/figures/{id}.png (or .pdf/.jpg). The source file path in FIGURES.md has been set to figures/{id}.png -- update it in FIGURES.md if you use a different extension. Static images are included directly by LaTeX via \\includegraphics."

**Step 4: Generate LaTeX Snippet**
Generate and present the LaTeX snippet the researcher should insert into their chapter .tex file:

For **excalidraw** type:
```latex
% Figure: {caption} (Chapter {chapter})
\begin{figure}[htb]
  \caption{\label{fig:{id}}{caption}}
  \begin{center}
    \includegraphics[width=0.8\textwidth]{exports/{id}}
  \end{center}
  \legend{Fonte: os autores}
\end{figure}
```

For **static** type (note: no `exports/` prefix -- static files are in `figures/` directly):
```latex
% Figure: {caption} (Chapter {chapter})
\begin{figure}[htb]
  \caption{\label{fig:{id}}{caption}}
  \begin{center}
    \includegraphics[width=0.8\textwidth]{{id}}
  \end{center}
  \legend{Fonte: os autores}
\end{figure}
```

For **tikz** type:
```latex
% Figure: {caption} (Chapter {chapter})
\begin{figure}[htb]
  \caption{\label{fig:{id}}{caption}}
  \begin{center}
    \input{figures/{id}.tikz}
  \end{center}
  \legend{Fonte: os autores}
\end{figure}
```

Present the snippet with instructions:
- "Insert this LaTeX snippet into your Chapter {chapter} .tex file where you want the figure to appear."
- "The `\caption{\label{}}` comes BEFORE the image (abnTeX2/ABNT convention: caption above the figure)."
- "Update `\legend{Fonte: os autores}` with the actual source attribution if needed."
- "Reference this figure in text with: `conforme ilustrado na \autoref{fig:{id}}`"

**Step 5: Run Validation**
After registration, run figure validation to check overall health:
```bash
node ~/.claude/get-thesis-done/bin/gtd-tools.js validate-figs --raw
```
Present results to the user:
- If 0 missing references: "All figure references across all chapters have corresponding FIGURES.md entries."
- If missing references exist: list them per chapter with a suggestion to register the missing figures.
- If unreferenced catalog entries exist: note them as informational ("These figures are registered but not yet referenced in any chapter" -- this is expected for newly registered figures).

**Step 6: Commit Changes**
Commit the FIGURES.md update and any created source files:
```bash
git add .planning/FIGURES.md src/figures/
git commit -m "fig: add figure {id} ({type}) for chapter {chapter}"
```
Report the commit to the user.

**Step 7: Summary**
Present a final summary:
- Figure ID and type registered
- Source file created/expected path
- LaTeX snippet (repeated for easy copy)
- Validation status
- Next steps:
  - For excalidraw: "Open src/figures/{id}.excalidraw in Excalidraw, create your diagram, then run /gtd:compile to export and build."
  - For tikz: "Edit src/figures/{id}.tikz with your TikZ code, then run /gtd:compile to build."
  - For static: "Place your image at src/figures/{id}.png, then run /gtd:compile to build."
  - For all: "Insert the LaTeX snippet into your chapter .tex file. The figure will appear in the compiled PDF."

**Important workflow constraints:**
- The workflow does NOT spawn the figure-manager agent for simple registrations. The workflow handles the complete flow directly.
- The workflow handles one figure at a time. For batch registration, the user runs `/gtd:add-figure` multiple times.
- All error messages use thesis-native terminology.
- The workflow never modifies chapter .tex files -- it presents snippets for the researcher to insert.
- The workflow uses `\autoref{fig:*}` in examples (abnTeX2 convention) rather than `\ref{fig:*}`.
  </action>
  <verify>
    <automated>test -f get-thesis-done/workflows/add-figure.md && grep -c 'register-figure\|validate-figs' get-thesis-done/workflows/add-figure.md</automated>
  </verify>
  <done>
- Workflow file exists at get-thesis-done/workflows/add-figure.md
- Contains purpose, core_principle, terminology, and process sections matching project conventions
- Step 1 gathers all 4 required details (ID, type, chapter, caption) with fallback questions
- Step 2 delegates to register-figure CLI command
- Step 3 creates source file scaffold appropriate to each type (excalidraw JSON, tikz template, static instructions)
- Step 4 generates correct LaTeX snippets with abnTeX2 caption-above pattern
- Step 5 runs validate-figs for post-registration health check
- Step 6 commits changes to git
- Step 7 presents summary with type-specific next steps
- References both CLI commands by exact name (register-figure, validate-figs)
  </done>
</task>

<task type="auto">
  <name>Task 2: Create /gtd:add-figure command definition</name>
  <files>commands/gtd/add-figure.md</files>
  <action>
Create `commands/gtd/add-figure.md` following the exact pattern of `commands/gtd/add-reference.md`.

**Frontmatter:**
```yaml
---
name: gtd:add-figure
description: Create and register a figure in the thesis figure catalog with source file scaffolding
argument-hint: "<figure-id> [--type <excalidraw|tikz|static>] [--chapter <N>] [--caption <text>]"
allowed-tools:
  - Read
  - Write
  - Bash
  - Glob
  - Grep
  - AskUserQuestion
---
```

**<objective> section:**
Register a new figure in the thesis FIGURES.md catalog, create the appropriate source file scaffold, and generate the LaTeX inclusion snippet. Auto-gathers any missing details interactively.

Context budget: ~25% orchestrator -- CLI handles registration and validation, workflow handles source file creation and snippet generation.

**<execution_context> section:**
```
@~/.claude/get-thesis-done/workflows/add-figure.md
```

**<context> section:**
```
Arguments: $ARGUMENTS
```

**<process> section:**
1. Execute the add-figure workflow from `@~/.claude/get-thesis-done/workflows/add-figure.md` end-to-end. Pass `$ARGUMENTS` as the input.
2. The workflow will:
   - Parse or interactively gather figure details (ID, type, chapter, caption)
   - Register the figure in FIGURES.md via CLI (register-figure command)
   - Create the source file scaffold (excalidraw JSON, tikz template, or static placement instructions)
   - Generate the LaTeX inclusion snippet with correct abnTeX2 ordering
   - Run figure validation (validate-figs)
   - Commit changes to git
3. On completion, report all results:
   - Registered figure details (ID, type, chapter, source file path)
   - LaTeX snippet for chapter inclusion
   - Validation status (all figure references healthy or issues found)
   - Next steps (edit source file, insert snippet, compile)
  </action>
  <verify>
    <automated>test -f commands/gtd/add-figure.md && grep -c 'gtd:add-figure' commands/gtd/add-figure.md && grep -c 'add-figure.md' commands/gtd/add-figure.md</automated>
  </verify>
  <done>
- Command file exists at commands/gtd/add-figure.md
- Frontmatter has name, description, argument-hint, allowed-tools matching project conventions
- References the add-figure workflow in execution_context
- Passes $ARGUMENTS to workflow
- allowed-tools includes AskUserQuestion (for interactive detail gathering)
  </done>
</task>

</tasks>

<verification>
1. Workflow exists and references both CLI commands: `grep -cE 'register-figure|validate-figs' get-thesis-done/workflows/add-figure.md` (should be >= 2)
2. Command exists and references workflow: `grep 'add-figure.md' commands/gtd/add-figure.md`
3. Workflow has required sections: `grep -cE '<purpose>|<core_principle>|<terminology>|<process>' get-thesis-done/workflows/add-figure.md` (should be 4)
4. Command has required frontmatter: `grep -cE 'name:|description:|argument-hint:|allowed-tools:' commands/gtd/add-figure.md` (should be 4)
5. Workflow covers all 3 figure types: `grep -cE 'excalidraw|tikz|static' get-thesis-done/workflows/add-figure.md` (should be >= 3)
6. LaTeX snippet includes abnTeX2 pattern: `grep -c 'caption.*label.*fig:' get-thesis-done/workflows/add-figure.md` (should be >= 1)
7. No references to GWD terminology in main content: `grep -ciE 'gwd|get-writing-done|BIBLE\.md' get-thesis-done/workflows/add-figure.md` (should be 0, except in terminology NOT-This column)
</verification>

<success_criteria>
- /gtd:add-figure command is defined and references the add-figure workflow
- Workflow gathers figure details (ID, type, chapter, caption) with interactive fallback
- Workflow delegates registration to register-figure CLI command
- Workflow creates source file scaffolds for all 3 figure types
- Workflow generates correct LaTeX snippets with abnTeX2 caption-above pattern
- Workflow runs validate-figs after registration
- Workflow commits FIGURES.md and source file changes to git
- Workflow uses thesis-native terminology throughout
- Workflow presents type-specific next steps to the researcher
- All 5 FIG-* requirements are covered by the command+workflow combination
</success_criteria>

<output>
After completion, create `.planning/phases/05-figure-management/05-02-SUMMARY.md`
</output>
